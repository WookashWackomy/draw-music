import React from "react";
import PropTypes from "prop-types";

const Header = props => {
    return (
        <div>
            <h4>header</h4>
        </div>
    );
};

Header.propTypes = {};

export default Header;
