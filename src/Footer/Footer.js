import React from "react";
import PropTypes from "prop-types";

const Footer = props => {
    return (
        <div>
            <h4>footer</h4>
        </div>
    );
};

Footer.propTypes = {};

export default Footer;
